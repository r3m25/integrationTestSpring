# Connection
spring:
  datasource:
    url: jdbc:h2:mem:spring-boot-lab-sab
    driver-class-name: org.h2.Driver
    username: test
    password: integration
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop

server:
  port: 8082